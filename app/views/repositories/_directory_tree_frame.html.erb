<turbo-frame id="dir_<%= parent_path.parameterize(separator: '_') %>">
  <%= render partial: 'directory_tree', locals: { tree: tree, level: (defined?(level) ? level : 1), parent_path: parent_path } %>
</turbo-frame> 